# 측정할 때 Playwright 를 쓰려고 /opt/node22/lib/node_modules 로 심볼릭 링크를 건다.
# 이게 실수로 커밋된 적이 있는데(rev 125), 배포하는 러너에는 그 경로가 없어서
# upload-pages-artifact 의 tar 가 "File removed before we read it" 으로 죽었다.
# 사이트에는 빌드 도구가 없으므로 여기 걸리는 것은 전부 측정용 부산물이다.
node_modules/
node_modules
